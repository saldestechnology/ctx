# ctx for Codex

Generated by `ctx harness init --target codex --mode plugin` (ctx v{{CTX_VERSION}}).

Add this directory as a local marketplace with `codex plugin marketplace add ./`,
then install the `ctx` plugin. Review and trust its lifecycle hooks with `/hooks`.
The hooks provide a session map, post-edit architecture checks, and a stop-time
quality score against `{{DEFAULT_BRANCH}}`.
